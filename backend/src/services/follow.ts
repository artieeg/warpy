export const FollowService = {
  async createNewFollow(follower: string, userToFollow: string): Promise<void> {
    return;
  },

  async removeFollow(follower: string, userToUnfollow: string): Promise<void> {
    return;
  },
};
