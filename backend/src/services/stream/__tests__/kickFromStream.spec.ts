import { StreamService } from "../index";

describe("StreamService.kickFromStream", () => {
  it.todo("requests media server to remote user's media streams");
  it.todo("updates isBanned field on the participant model");
  it.todo("broadcasts kicked user to other participants");
});
