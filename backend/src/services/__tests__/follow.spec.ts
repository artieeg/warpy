import { FollowService } from "../follow";

describe("FollowService", () => {
  it.todo("creates new follow record");
  it.todo("deletes a follow record");
  it.todo("sends a notification on a new follow");
});
