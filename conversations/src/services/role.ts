import { Roles } from "@app/types";

/* Sets a role for user */
export const setRole = async (user: string, role: Roles) => {};

/* Removes role record */
export const clearRole = async (user: string) => {};
