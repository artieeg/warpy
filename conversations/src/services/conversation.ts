import { IStream } from "@app/models";

export const handleNewConversation = async (stream: IStream) => {};
export const handleConversationEnd = async (streamId: string) => {};
export const handleParticipantLeft = async (user: string) => {};
